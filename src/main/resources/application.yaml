server:
  port: 5000

spring:
  application:
    name: shop-service
  profiles:
    active: dev
  config:
    import: "optional:configserver:"

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/
    register-with-eureka: true
    fetch-registry: true